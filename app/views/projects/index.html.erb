<h1 class="text-2xl font-bold mb-6 text-center">Projects</h1>

<%= turbo_frame_tag id='project_form', src:new_project_path %>
<ul class="space-y-4 mb-4" id="projects">
  <% @projects.each do |project| %>
    <%= render 'project', project: project%>
  <% end %>
</ul>
